<% title "Adicionar Produto" %>
<div class="box4">
  <div class="breadcrumb margin_top">
    <strong>Nome e Descrição</strong>
    &gt; Postar
  </div>

    <div class="flash_box">
  <%= render 'shared/flash_messages' %>
  <%= render 'shared/error_messages', :target => @product %>
</div>


  <%= form_for @product  do |p| %>
  <div class="block margin_top">
    <h1>Adicionar Produto</h1>
<div class="content">
<div class="subcontent_top">
    <h4>Categorias</h4>


      <ul>
        <li>

          <#%= p.select :gender_id, options_for_select(@genders.collect { |gender|
          [gender.gender.titleize, gender.id] }, 1), {}, { id: 'genders', name: 'genders'} %>
  <%=  p.collection_select :gender_id, Gender.all, :id, :gender %>
        </li>

        <li>

          <#%= p.select :category_id, options_for_select(@categories.collect { |category|
              [category.name.titleize, category.id] }, 0), {}, { id: 'categories', name: 'categories'} %>
  <%=  p.collection_select :category_id, Category.all, :id, :name %>


        </li>




        <li>

          <#%= p.select :subcategory_id, options_for_select(@subcategories.collect { |subcategory|
                  [subcategory.name.titleize, subcategory.id] }, 0), {}, { id: 'subcategories_select' } %>
  <%=  p.collection_select :subcategory_id, Subcategory.all, :id, :name %>
        </li>
      </ul>


  </div>

  <div class="subcontent">
    <h4>Nome e Descrição</h4>

      <ul>
        <li>
          <%= p.text_field  :name, :label =>false, :placeholder => "Nome"%>
        </li>
        <li>
          <div id="notes-bar"></div>
          <%= p.text_area :detail,  :id => 'notes', :placeholder => "Descrição"%>
          <div id="notes-preview"></div>
        </li>
      </ul>

  </div>


<div class="subcontent">

  <h4>Especificações</h4>

     <span><b>Tamanho</b></span>
     <%= hidden_field_tag "product[size_ids][]", nil %>
     <% Size.all.each do |size| %>
     <%= check_box_tag "product[size_ids][]", size.id, @product.size_ids.include?(size.id), id: dom_id(size) %>
     <%= label_tag dom_id(size), size.size %><br>
     <% end %>

    




     <span><b>Cor</b></span>
     <%= hidden_field_tag "product[color_ids][]", nil %>
     <% Color.all.each do |color| %>
     <%= check_box_tag "product[color_ids][]", color.id, @product.color_ids.include?(color.id), id: dom_id(color) %>
     <%= label_tag dom_id(color), color.name %><br>
     <% end %>
     <ul>
     <b>Material</b>&nbsp;
     <%=  p.collection_select :material_id, Material.all, :id, :name %>
     </li>
     <li>
        <b>Condição</b>&nbsp;
        <%=  p.collection_select :condition_id, Condition.all, :id, :name %>
     </li>
   </div>

  <div class="subcontent">


                 <h4>Preço e Quantidade</h4>
<ul>
  <li>
                    <%= p.number_field :quantity, :label =>"Quantidade" %>
                  </li>
                  <li>

                    <%= p.text_field :price, :label =>"Preço R$" %>


</li>
</ul>



  </div>


</div>

</div>
<%= p.button  "Próximo", :class => "btn_orange btn_big" %>

<% end %>
</div>
<div class="box11">
  <div class="tip">
    <h4>Fotos</h4>
    <p>Adicione no mínimo 2 fotos</p>
    <p>
      Se você tem dúvida de como tirar suas fotos acesse o link
      <%= link_to 'Fotografia', page_path('fotografia') %></p>
  </div>
  <div class="tip">
    <h4>Palavras Chave</h4>
    <p>Adicione no máximo 5 palavras chave</p>
    <p>As palavras chave facilitam o google e bing a achar de seu produto com mais facilidade
    </p>
  </div>
  <div class="tip">
    <h4>Medidas</h4>
    <p>
      Adicione apenas as medidas pertinente ao produto OBS: Todas as medidas serão em Centímetros Comprimento: 67,5cm
    </p>
  </div>
</div>
