<div class="contextual">
  <%= link_to t('helpers.links.show'), [:admin, @order], :class => 'icon icon-view' %>
  <%= link_to t('helpers.links.destroy'), [:admin, @order], :confirm => t('helpers.links.confirm'), :method => :delete, :class => 'icon icon-del' %>
</div>

<h2>Editing Order</h2>

<%= simple_form_for(@order, :url => [:admin, @order], :html => {:class => 'tabular'}) do |form| %>
  <%= form.error_messages %>

  <%= render :partial => form %>

  <%= form.submit t('helpers.submit.update') %>
  <%= link_to t('helpers.links.back'), admin_orders_path %>
<% end %>
