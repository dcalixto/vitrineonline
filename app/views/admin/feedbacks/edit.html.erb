<div class="contextual">
  <%= link_to t('helpers.links.show'), [:admin, @feedback], :class => 'icon icon-view' %>
  <%= link_to t('helpers.links.destroy'), [:admin, @feedback], :confirm => t('helpers.links.confirm'), :method => :delete, :class => 'icon icon-del' %>
</div>

<h2>Editing Feedback</h2>

<%= simple_form_for(@feedback, :url => [:admin, @feedback], :html => {:class => 'tabular'}) do |form| %>
  <%= form.error_messages %>

  <%= render :partial => form %>

  <%= form.submit t('helpers.submit.update') %>
  <%= link_to t('helpers.links.back'), admin_feedbacks_path %>
<% end %>
