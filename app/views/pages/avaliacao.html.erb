<%= render "shared/ajuda_menu" %>

<div class="box6">
  <div class="block margin_top">
    <h4>Avaliação</h4>
    <div class="content">
      O sistema de avaliação das compras e vendas na vitrineonline é baseada em estrelas
      <ol>
        <li>
          <%= content_tag(:span, nil, :class =>"sprite-icons-1star") %>

       = péssimo
        </li>
        <li>
           <%= content_tag(:span, nil, :class =>"sprite-icons-2star") %>

         = ruim
        </li>
        <li>
           <%= content_tag(:span, nil, :class =>"sprite-icons-3star") %>

        = regular
        </li>
        <li>
           <%= content_tag(:span, nil, :class =>"sprite-icons-4star") %>

        = bom
        </li>
        <li>
           <%= content_tag(:span, nil, :class =>"sprite-icons-5star") %>

         = excelente
        </li>

      </ol>
    </div>

  </div>
</div>
