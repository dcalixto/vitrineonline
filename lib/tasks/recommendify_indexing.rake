#task :recommendify_indexing => :environment do |x, args|
 # recommender = Recommender.new

  # 2つ以上を同時に投入しないと意味がない。
#  recommender.impression_items.add_set("impression2", ["product3", "product5", "productm6"])
#  recommender.impression_items.add_set("impresssion1", ["product7", "product8"])


#  recommender.process!

#end
